# Synthetic demo data

Generated output belongs in `data/generated/` and is ignored by Git. Use only the deterministic generator and clearly synthetic records. All units and values are arbitrary demonstration values; they are not real aircraft measurements or validated engineering limits. Ground-truth labels are only for offline evaluation.
